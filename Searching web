# Install: pip install duckduckgo-search
from duckduckgo_search import DDGS

with DDGS() as ddgs:
    results = [r for r in ddgs.text("your query", max_results=5)]
    for result in results:
        print(result)
from langchain_community.tools import DuckDuckGoSearchRun
search = DuckDuckGoSearchRun()
output = search.run("your query")
print(output)
// Search URL: https://duckduckgo.com/html/?q=query
Document doc = Jsoup.connect(DUCKDUCKGO_SEARCH_URL + query).get();
Elements results = doc.getElementById("links").getElementsByClass("results_links");

